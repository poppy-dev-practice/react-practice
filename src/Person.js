import React from 'react'

function Person({person}) {
  return (
    <div>
        <h2>op is {person.id} iop {person.name}gjsy {person.clg}xfgjsyk {person.empolyment}</h2>
    </div>
  )
}

export default Person